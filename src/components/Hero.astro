---
import { Icon } from 'astro-icon/components'

const { src = '/web_designer_boy.webp' } = Astro.props
---

<section class="hero my-24">
  <div class="container">
    <div class="grid grid-cols-1 items-center gap-24 lg:grid-cols-2">
      <div class="flex flex-col items-center gap-8 md:items-start">
        <h1 class="text-center text-6xl md:text-left lg:text-8xl">
          <slot><span class="text-gradient">Saludos!</span></slot>
        </h1>
        <p class="text-2xl">
          ¡Aloha desarrolladores del mañana!. Soy Öslar, y estoy aquí para convertir el HTML en 'How To Make Life' y el
          CSS en 'Creating Stunning Sites'
        </p>
        <div class="flex flex-col gap-3 min-[500px]:flex-row">
          <a class="button has-icon" href="https://github.com/artificacial" target="_blank">
            <Icon name="mdi:github" />
            See you on GitHub
          </a>
          <a class="button has-icon color-secondary" href="#" hidden>
            <Icon name="mdi:file-document-outline" />
            Read the Docs
          </a>
        </div>
      </div>
      <img class="hidden lg:block" src={src} alt="" decoding="async" />
    </div>
  </div>
</section>

<style lang="scss">
  .text-gradient {
    background: linear-gradient(315deg, var(--primary-100) 25%, var(--primary-100));
    background-clip: border-box;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  :global(.darkmode .text-gradient) {
    background: linear-gradient(315deg, var(--secondary-100) 25%, var(--secondary-100));
    background-clip: border-box;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }
</style>
